# ARCWELL frontend

React 19 and TanStack Start application with an investing research workspace,
evidence-registry demo, and read-only Arc Testnet panels.

From the repository root, run `npm run setup` and `npm run dev`.
See the [main README](../README.md) for capabilities and operating boundaries.
